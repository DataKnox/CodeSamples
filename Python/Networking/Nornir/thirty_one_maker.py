import ipaddress
import requests
from art import *

# base_net = ipaddress.ip_network('192.168.1.0/24')
# subnets = list(base_net.subnets(new_prefix=31))
# subnets = subnets[1:-1]
# for subnet in subnets:
#     print(subnet)
tprint("Data Knox")
